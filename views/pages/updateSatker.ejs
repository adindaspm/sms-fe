
        <div class="mb-6">
            <h1 class="text-4xl text-font font-extrabold dark:text-gray-100">Update Satuan Kerja</h1>
        </div>
        <form class="w-full mx-auto" role="form" method="post" action="/satkers/<%= satker.id %>/update">
            <input type="hidden" name="id" value="<%= satker.id %>">
            <div class="w-full p-6 bg-primary-30 border border-gray-200 rounded-lg shadow dark:bg-gray-800 dark:border-gray-700">
                <div class="relative z-0 w-full mb-5 group">
                    <input type="text" name="name" id="name" value="<%= typeof old?.name !== 'undefined' ? old.name : satker.name %>" class="block py-2.5 px-0 w-full text-sm text-gray-900 bg-transparent border-0 border-b-2 border-gray-300 appearance-none dark:text-gray-100 dark:border-gray-600 dark:focus:border-blue-500 focus:outline-none focus:ring-0 focus:border-blue-600 peer" placeholder=" " />
                    <label for="name" class="absolute text-sm text-gray-900 dark:text-gray-400 duration-300 transform -translate-y-6 scale-75 top-3 -z-10 origin-[0] peer-focus:start-0 peer-focus:font-medium peer-focus:dark:text-blue-500 peer-placeholder-shown:scale-100 peer-placeholder-shown:translate-y-0 peer-focus:scale-75 peer-focus:-translate-y-6 rtl:peer-focus:translate-x-1/4 rtl:peer-focus:left-auto">Nama</label>
                    <% if (errors) { 
                        const err = errors.find(e => e.path === 'name'); %>
                        <% if (err) { %>
                            <p class="text-red-700 text-sm mt-1"><%= err.msg %></p>
                        <% } %>
                    <% } %>
                </div>
                <div class="relative z-0 w-full mb-5 group">
                    <input type="text" name="code" id="code" value="<%= typeof old?.code !== 'undefined' ? old.code : satker.code %>" class="block py-2.5 px-0 w-full text-sm text-gray-900 bg-transparent border-0 border-b-2 border-gray-300 appearance-none dark:text-gray-100 dark:border-gray-600 dark:focus:border-blue-500 focus:outline-none focus:ring-0 focus:border-blue-600 peer" placeholder=" " />
                    <label for="code" class="absolute text-sm text-gray-900 dark:text-gray-400 duration-300 transform -translate-y-6 scale-75 top-3 -z-10 origin-[0] peer-focus:start-0 peer-focus:font-medium peer-focus:dark:text-blue-500 peer-placeholder-shown:scale-100 peer-placeholder-shown:translate-y-0 peer-focus:scale-75 peer-focus:-translate-y-6 rtl:peer-focus:translate-x-1/4 rtl:peer-focus:left-auto">Kode</label>
                    <% if (errors) { 
                        const err = errors.find(e => e.path === 'code'); %>
                        <% if (err) { %>
                            <p class="text-red-700 text-sm mt-1"><%= err.msg %></p>
                        <% } %>
                    <% } %>
                </div>
                <div class="relative z-0 w-full mb-5 group">
                    <input type="text" name="address" id="address" value="<%= typeof old?.address !== 'undefined' ? old.address : satker.address %>" class="block py-2.5 px-0 w-full text-sm text-gray-900 bg-transparent border-0 border-b-2 border-gray-300 appearance-none dark:text-gray-100 dark:border-gray-600 dark:focus:border-blue-500 focus:outline-none focus:ring-0 focus:border-blue-600 peer" placeholder=" " />
                    <label for="address" class="peer-focus:font-medium absolute text-sm text-gray-900 dark:text-gray-400 duration-300 transform -translate-y-6 scale-75 top-3 -z-10 origin-[0] peer-focus:start-0 rtl:peer-focus:translate-x-1/4 peer-focus:font-medium peer-focus:dark:text-blue-500 peer-placeholder-shown:scale-100 peer-placeholder-shown:translate-y-0 peer-focus:scale-75 peer-focus:-translate-y-6">Alamat</label>   
                    <% if (errors) { 
                        const err = errors.find(e => e.path === 'address'); %>
                        <% if (err) { %>
                            <p class="text-red-700 text-sm mt-1"><%= err.msg %></p>
                        <% } %>
                    <% } %>
                </div>
                <div class="relative z-0 w-full mb-5 group">
                    <input type="text" name="number" id="number" value="<%= typeof old?.number !== 'undefined' ? old.number : satker.number %>" class="block py-2.5 px-0 w-full text-sm text-gray-900 bg-transparent border-0 border-b-2 border-gray-300 appearance-none dark:text-gray-100 dark:border-gray-600 dark:focus:border-blue-500 focus:outline-none focus:ring-0 focus:border-blue-600 peer" placeholder=" " />
                    <label for="number" class="peer-focus:font-medium absolute text-sm text-gray-900 dark:text-gray-400 duration-300 transform -translate-y-6 scale-75 top-3 -z-10 origin-[0] peer-focus:start-0 rtl:peer-focus:translate-x-1/4 peer-focus:font-medium peer-focus:dark:text-blue-500 peer-placeholder-shown:scale-100 peer-placeholder-shown:translate-y-0 peer-focus:scale-75 peer-focus:-translate-y-6">Nomor Telepon/Faksimile</label>
                    <% if (errors) { 
                        const err = errors.find(e => e.path === 'number'); %>
                        <% if (err) { %>
                            <p class="text-red-700 text-sm mt-1"><%= err.msg %></p>
                        <% } %>
                    <% } %>
                </div>
                <div class="relative z-0 w-full mb-5 group">
                    <input type="text" name="email" id="email" value="<%= typeof old?.email !== 'undefined' ? old.email : satker.email %>" class="block py-2.5 px-0 w-full text-sm text-gray-900 bg-transparent border-0 border-b-2 border-gray-300 appearance-none dark:text-gray-100 dark:border-gray-600 dark:focus:border-blue-500 focus:outline-none focus:ring-0 focus:border-blue-600 peer" placeholder=" " />
                    <label for="email" class="peer-focus:font-medium absolute text-sm text-gray-900 dark:text-gray-400 duration-300 transform -translate-y-6 scale-75 top-3 -z-10 origin-[0] peer-focus:start-0 rtl:peer-focus:translate-x-1/4 peer-focus:font-medium peer-focus:dark:text-blue-500 peer-placeholder-shown:scale-100 peer-placeholder-shown:translate-y-0 peer-focus:scale-75 peer-focus:-translate-y-6">Email</label>
                    <% if (errors) { 
                        const err = errors.find(e => e.path === 'email'); %>
                        <% if (err) { %>
                            <p class="text-red-700 text-sm mt-1"><%= err.msg %></p>
                        <% } %>
                    <% } %>
                </div>
                <div class="relative z-0 w-full mb-5 group">
                    <select name="province" id="province"  
                    class="block py-2.5 px-0 w-full text-sm text-gray-900 bg-transparent border-0 border-b-2 border-gray-300 dark:text-gray-100 focus:outline-none focus:ring-0 peer">
                        <option value="<%= encodeURIComponent(JSON.stringify(satker.province)) %>" class="bg-primary-30"><%= satker.province.name %></option>
                        <% provinceDtos.forEach(p => { 
                            const isSelected = old?.province && JSON.parse(decodeURIComponent(old.province)).id === p.id; %>
                            <option class="bg-primary-30" value="<%= encodeURIComponent(JSON.stringify(p)) %>" <%= isSelected ? 'selected' : '' %>><%= p.name %></option>
                        <% }) %>
                    </select>
                    <label for="province" class="absolute text-sm text-gray-900 dark:text-gray-400 duration-300 transform -translate-y-6 scale-75 top-3 -z-10 origin-[0] peer-focus:start-0 peer-focus:dark:text-blue-500 peer-placeholder-shown:scale-100 peer-placeholder-shown:translate-y-0 peer-focus:scale-75 peer-focus:-translate-y-6 rtl:peer-focus:translate-x-1/4 rtl:peer-focus:left-auto">Provinsi</label>
                    <% if (errors) { 
                        const err = errors.find(e => e.path === 'province'); %>
                        <% if (err) { %>
                            <p class="text-red-700 text-sm mt-1"><%= err.msg %></p>
                        <% } %>
                    <% } %>
                </div>
            </div>
            <div class="flex justify-between">
                <div class="mt-6">
                    <button type="submit" class="text-gray-200 bg-blue-700 hover:bg-blue-800 focus:ring-4 focus:outline-none focus:ring-blue-300 font-medium rounded-lg text-sm w-full sm:w-auto px-5 py-2.5 text-center dark:bg-blue-600 dark:hover:bg-blue-700 dark:focus:ring-blue-800">Submit</button>            
                </div>
                <div class="mt-6">
                    <a href="/satkers">
                        <button type="button" class="text-gray-200 bg-blue-700 hover:bg-blue-800 focus:ring-4 focus:outline-none focus:ring-blue-300 font-medium rounded-lg text-sm w-full sm:w-auto px-5 py-2.5 text-center dark:bg-blue-600 dark:hover:bg-blue-700 dark:focus:ring-blue-800">Kembali</button>
                    </a>
                </div>
            </div>
        </form>